{
  "cache_dir": "${HOME}/.cache/delve",
  "log_dir": "${HOME}/.claude/delve/logs",
  "audit_log": "${HOME}/.claude/delve/audit.log",
  "citations_db": "${HOME}/.claude/delve/citations.json",
  "pdf_cache": "${PLATFORM_CACHE}/pdfs",
  "session_dir": "${PROJECT_ROOT}/research-sessions",
  "knowledge_base_core": "${PROJECT_ROOT}/knowledge-base",
  "knowledge_base_custom": "${PROJECT_ROOT}/knowledge-base-custom",
  "config_dir": "${PROJECT_ROOT}/config",
  "agents_dir": "${PROJECT_ROOT}/.claude/agents",
  "hooks_dir": "${PROJECT_ROOT}/.claude/hooks"
}