# User customizations (stored in user home directory, not in project)
knowledge-base-custom/

# Test user config (for testing user-specific agents/missions)
user-test-config/

# Internal documentation
internal_docs/

# Session data (can be large)
# Note: Sessions include their own .claude/ directories and .mcp.json
research-sessions/
# Specifically ignore session-specific configs
research-sessions/*/.claude/settings.local.json
research-sessions/*/.mcp.json

# Logs
logs/
*.log

# Cache
.cache/

# Temp files
*.tmp
*.lock

# History
.history/

# OS files
.DS_Store

# Note: User configs are stored in ~/.config/delve/ (not in project)
# Config files: only .default.json should be tracked
config/*.json
!config/*.default.json

# AI assistant memory/context (internal only, not for repo)
memory-bank/

# Generated reports (test outputs, not documentation)
reports/

# POC and experimental directories
poc-subagent/
test-json-format-only/
test-multi-task/
test-clean-dir/

# Temporary fix/debug scripts
fix-agent-json*.sh
fix-json-multiline.py
rebuild-agent-json.py
test-all-agents.sh
test-phase0-simple.sh

# Backup files (includes perl -i.bak generated backups)
*-backup-*/
*.backup-*
*.backup
*.bak*
